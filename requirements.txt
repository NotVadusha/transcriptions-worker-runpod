# Worker image dependencies — ONLY what the NeMo NGC base image lacks.
#
# torch, nemo_toolkit, CUDA, libsndfile, numpy, etc. are ALL provided by the
# base image (nvcr.io/nvidia/nemo:25.09.02 — see Dockerfile). Do NOT pin or
# reinstall torch/nemo here: that risks breaking the GPU-matched build the NeMo
# image ships (Blackwell/Hopper compute capability + CUDA 12.9 + torch 2.8).
#
# whisper / assemblyai are benchmark-only and are NOT installed in the worker
# image — keep them out of here.
runpod>=1.6.0
httpx>=0.27

# Multi-model routing (SPEC: language -> backend).
#   funasr        -> SenseVoiceSmall backend (Asian languages).
#   transformers  -> whisper-large-v3 backend (catch-all). SHIPS WITH the NeMo
#                    base image (NeMo depends on it) — pinned here only as a
#                    floor; do NOT let pip downgrade the image's torch. Canary is
#                    NeMo, already provided by the base image.
# MUST-VALIDATE-ON-GPU: confirm funasr installs cleanly against the pinned
# torch/CUDA and does not drag in a conflicting torch.
funasr>=1.1.0
transformers>=4.44
